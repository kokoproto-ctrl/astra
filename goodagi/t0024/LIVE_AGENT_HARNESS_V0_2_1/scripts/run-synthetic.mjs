import fs from 'node:fs'; import path from 'node:path'; import {ControlBoundary} from '../src/boundary.mjs'; import {runThreeAgents,writeJsonl} from '../src/runner.mjs';
const root=path.resolve(import.meta.dirname,'..'); const s=JSON.parse(fs.readFileSync(path.join(root,'scenarios/three_agents.json'))); const boundary=new ControlBoundary({principals:s.agents.map(a=>({id:a.principal_id,task_id:a.task_id,budget:a.budget,allowed_actions:['inspect_object','request_resource','send_synthetic_message','submit_answer']})),objects:s.objects});
const r=await runThreeAgents({boundary,agents:s.agents,model:'synthetic-non-llm-fixture',promptText:fs.readFileSync(path.join(root,'prompts/agent_system.txt'),'utf8'),evidenceDir:path.join(root,'evidence'),invoke:async(a,t)=>({effect_id:`${a.agent_id}-${t}`,action:t===0?'inspect_object':'submit_answer',object_id:'shared-resource',cost:1,payload:{}})});
fs.writeFileSync(path.join(root,'evidence/synthetic_run_results.json'),JSON.stringify({synthetic_run:'PASS',agents:3,decisions:r.decisions.length},null,2)); writeJsonl(path.join(root,'evidence/boundary_events.jsonl'),boundary.evidence);
