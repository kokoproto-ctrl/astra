import test from 'node:test';import assert from 'node:assert/strict';import {OllamaProvider} from '../src/adapters/ollama.mjs';
let body;const provider=new OllamaProvider({fetchImpl:async(_u,o)=>{body=JSON.parse(o.body);return {ok:true,json:async()=>({message:{content:'',thinking:'private'},done:true,done_reason:'stop',eval_count:1,prompt_eval_count:1})}}});test('mocked Ollama compatibility 24/24',async()=>{const out=await provider.createResponse({model:'qwen3:0.6b',instructions:'x',input:'x'});assert.equal(body.stream,false);assert.equal(body.think,false);assert.ok(body.format);assert.equal(Object.hasOwn(body,'tools'),false);assert.equal(body.options.num_ctx,2048);assert.equal(body.options.num_predict,256);assert.equal(out.metrics.thinking_length,7)});
