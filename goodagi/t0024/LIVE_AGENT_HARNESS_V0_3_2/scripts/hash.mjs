import crypto from 'node:crypto'; import fs from 'node:fs'; import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'); const out=[]; function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory() && e.name!=='node_modules')walk(p); else if(e.isFile()&&e.name!=='hashes.sha256')out.push(`${crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}  ${path.relative(root,p).replaceAll('\\','/')}`)}} walk(root); fs.writeFileSync(path.join(root,'evidence/hashes.sha256'),out.sort().join('\n')+'\n');
