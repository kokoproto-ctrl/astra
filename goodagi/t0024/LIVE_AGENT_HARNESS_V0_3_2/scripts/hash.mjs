import crypto from 'node:crypto'; import fs from 'node:fs'; import path from 'node:path';
const root=path.resolve(import.meta.dirname,'..'); const out=[]; function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);const relative=path.relative(root,p).replaceAll('\\','/');if(e.isDirectory() && e.name!=='node_modules')walk(p); else if(e.isFile()&&relative!=='evidence/hashes.sha256'&&relative!=='evidence/hash_verification_results.json')out.push(`${crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex')}  ${relative}`)}} walk(root); fs.writeFileSync(path.join(root,'evidence/hashes.sha256'),out.sort().join('\n')+'\n');
